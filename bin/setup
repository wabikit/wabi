#!/usr/bin/env bash
set -euo pipefail

echo "==> Installing root tooling"
bundle install

echo "==> Installing gem deps"
(cd gem && bundle install)

echo "==> Setup complete. Try:"
echo "    cd gem && bundle exec rspec"
