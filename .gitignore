# Ruby
*.gem
*.rbc
.bundle/
.config/
.yardoc/
# The gem is a library — its lockfile stays uncommitted (convention).
# The docs app + registry are deployable/tooling: their Gemfile.lock IS committed
# (Render's frozen bundler needs it).
/gem/Gemfile.lock
InstalledFiles
_yardoc/
coverage/
doc/
pkg/
spec/reports/
spec/examples.txt
test/tmp/
test/version_tmp/
tmp/
*.swp
.byebug_history
.rspec_status

# Editors
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Local env
.env
.env.local
.env.*.local
.mise.local.toml

# Build artifacts
node_modules/
registry/dist/
gem/pkg/

# Local planning (per author preference: never push specs/plans)
docs/superpowers/

# Brainstorming companion (visual mockups, persists locally)
.superpowers/

# Pagefind
docs/tmp/pagefind-input/
docs/package-lock.json
